import styled from "styled-components";

const newLocal = `
  margin-top: -1.25rem;
  height: 15vh;
  display: flex;
  justify-content: space-between;
  align-items: center;
`;
export const HeaderWrapper = styled.headernewLocal;
